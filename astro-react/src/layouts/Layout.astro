<!doctype html>
<html lang="en" suppressHydrationWarning>
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>c15t + Astro + React Playground</title>
	</head>
	<body>
		<slot />
	</body>
</html>

<style is:global>
	@import "tailwindcss";

	:root {
		--neutral: #ebebeb;
	}

	@theme inline {
		--color-neutral: var(--neutral);
	}

	/* Theme overrides (class applied on <html>) */
	html {
		--neutral: #ebebeb; /* default light mode */
	}

	html.dark {
		--neutral: #333; /* dark mode override */
		background-color: #121212;
		color: white;
	}

	html,
	body {
		margin: 0;
		width: 100%;
		height: 100%;
		font-family: Arial, Helvetica, sans-serif;
	}

	body {
		background-color: inherit;
		color: inherit;
	}
</style>
